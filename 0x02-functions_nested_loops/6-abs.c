#include "main.h"
/**
 *_abs - computes the absolute value of the integer argument
 *
 *@k: checks the number
 *
 * Return: 0
 */

int _abs(int k)
{

	if (k >= 0)
	{
		return (k);
	}
	else
		return (k * -1);
}
